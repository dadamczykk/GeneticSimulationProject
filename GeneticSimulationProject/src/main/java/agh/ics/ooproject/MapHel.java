package agh.ics.ooproject;

public class MapHel{

    public MapHel(int width, int height, int noPlants, int plantEnergy, int noNewPlants,
                  AbstractPlantGenerator plantGenerator, int noAnimals, int startingEnergy,
                  int sufficientEnergy, int consumedEnergy, int minimalMutations, int maximalMutations,
                  AbstractGenotype genotype, BehaviourType behaviour) {
//        super(width, height, noPlants, plantEnergy, noNewPlants, plantGenerator,
//                noAnimals, startingEnergy, sufficientEnergy, consumedEnergy, minimalMutations,
//                maximalMutations, genotype, behaviour);
    }
//    @Override
//    void initializeGrass() {
//        int randomX = (int) Math.floor(Math.random()*this.width);
//        int randomY = (int) Math.floor(Math.random()*this.height);
//        for (int i = 0; i < noPlants; i++){
//            while (this.grid[randomY][randomX].hasGrass){
//                randomX = (int) Math.floor(Math.random()*this.width);
//                randomY = (int) Math.floor(Math.random()*this.height);
//            }
//            this.grid[randomY][randomX].addElement(new ElementGrass(new Position(randomX, randomY), this.plantEnergy));
//        }
//    }
}
